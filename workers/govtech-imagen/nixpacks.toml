[phases.setup]
nixPkgs = ["cairo", "pkg-config", "gobject-introspection", "libffi"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python generate_govtech_graphics.py --serve"
